package service;

import model.Passenger;
import model.Route;
import model.Station;

public class RoutingService {
    public Route calcNewRoute(Route currentRoute, Passenger newPassenger) {

    }

    public Route calcNewRoute(Route currentRoute, Passenger p, Station start, Station target) {
    }
}
